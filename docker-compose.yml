# Placeholder — will be configured in Phase 6
services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    env_file: .env
    volumes:
      - ./data:/app/data
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
